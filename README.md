# File extension Organizer

This code will verify the local directory and move all PDFs older than six month to a separate folder, 
in future versions this code will recive as paranmeter the extension, the time and the folther name.
